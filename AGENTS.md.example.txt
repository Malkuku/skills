## 核心要求
本项目采取了多层spec结构，你需要先阅读spec结构了解核心需求。
`./Spec/Me2AI/`是产品经理编写的需求文档，定位是UI设计+架构设计，帮助我管理设计并向你传达架构思路，你只能阅读，不能修改。
`./Spec/AI2AI/`是AI架构师编写的架构文档，包含架构设计，已开发的模块的外部接口。定位是架构说明，不包含代码，帮助你自己把工程持续可维护的迭代下去，你需要在迭代任务开始之前完整地阅读，并在任务完成之后进行及时的更新维护。

阅读`.codex/skills/spec-driven-agent-workflow/SKILL.md`获取完整要求

优先检索`Spec/AI2AI/索引.md`了解项目，CodeBase仅在深入细节时阅读


## 文件编码
所有文档和源码默认使用 UTF-8。

在 Windows PowerShell 中读取中文 Markdown 文档时，必须显式指定 `-Encoding UTF8`，例如：

```powershell
Get-Content -Raw -Encoding UTF8 "Spec/AI2AI/索引.md"
```

不要先用默认编码读取中文文档再解释乱码；如果看到中文乱码，优先检查命令是否缺少 `-Encoding UTF8`。

## 编译与启动

优先使用 JetBrains MCP 执行项目编译、启动和关闭，不再优先手写。

- 编译/构建：优先调用 JetBrains MCP 的 `build_project`，需要指定模块或文件时传入对应参数。
- 启动当前项目/客户端：优先通过 JetBrains MCP 的 `get_run_configurations` 查找运行配置，再使用 `execute_run_configuration` 启动。
- 如果需要启动后再由 Agent 关闭进程，优先使用 JetBrains MCP 的 `xdebug_start_debugger_session` 启动，并使用 `xdebug_control_session` 的 `STOP` 关闭。
- JetBrains MCP 不可用时，直接中断并记录任务进度，同时说明无法使用 MCP 的原因。


**允许你根据要求判断自行使用子 Agent / 委派 / 并行 Agent 工作**
